describe('interface typescript', function () {
    it('Should support interface in typescript', function () {
        const seller = {
            id: 1,
            name: "Wildan"
        };
        console.info(seller);
    });
});
export {};
